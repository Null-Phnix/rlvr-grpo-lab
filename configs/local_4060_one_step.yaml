seed: 42
model_name_or_path: Qwen/Qwen2.5-0.5B-Instruct
dataset:
  name: openai/gsm8k
  config: main
  split: train
  limit: 16
  prompt_style: think_answer
output_dir: outputs/local_4060_one_step
training:
  max_steps: 1
  learning_rate: 0.000005
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 2
  num_generations: 2
  max_prompt_length: 512
  max_completion_length: 64
  temperature: 0.8
  top_p: 0.95
  logging_steps: 1
  save_steps: 1
  bf16: false
  fp16: true
  gradient_checkpointing: true
  use_vllm: false
  beta: 0.0
  loss_type: dr_grpo
lora:
  enabled: true
  r: 8
  alpha: 16
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
rewards:
  correctness_weight: 1.0
  format_weight: 0.2
